retargeting:
  type: position
  urdf_path: left/linkerhand_l6_left.urdf

  target_joint_names: [ 'pinky_mcp_pitch', 'ring_mcp_pitch', 'middle_mcp_pitch', 'index_mcp_pitch',
    'thumb_cmc_pitch', 'thunb_cmc_roll'  ]
  target_link_names: [ 'thumb_distal', 'index_distal', 'middle_distal', 'ring_distal', 'pinky_distal']

  target_link_human_indices: [ 4, 8, 12, 16, 20 ]
  add_dummy_free_joint: True

  # A smaller alpha means stronger filtering, i.e. more smooth but also larger latency
  # 1 means no filter while 0 means not moving
  low_pass_alpha: 1

  # To ignore the mimic joint tags in the URDF, set it to True
  ignore_mimic_joint: False
